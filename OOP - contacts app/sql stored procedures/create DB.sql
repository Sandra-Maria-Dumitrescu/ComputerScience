--create database Agenda
CREATE DATABASE Agenda;

--create table Contact
CREATE TABLE Contact (
    id int primary key is identity,
    name varchar(50),
    [first name]  varchar(50),
    phone varchar(50),
    email
);